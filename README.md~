# description

Simple program that reads json and csv files of some specified format and produces json-formatted output file.
For the simplicity all data manipulations are inside one class (in real world it should be splitted).

Program requires two parameters:
pathToDirectory => path to directory containing csv and json files
outputFile => result file name

Parameters example:
-pathToDirectory /home/ubuntu/workspace/project/data-manipulation/src/main/resources/input -outputFile output.txt

Use following command to run the program:
gradle execute


